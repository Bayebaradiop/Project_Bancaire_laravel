# Configuration PostgreSQL pour Render
# Branche de déploiement : production
# Plan GRATUIT (Free)

services:
  - type: web
    name: project-laravel
    env: docker
    plan: starter  # Plan gratuit
    branch: production
    dockerfilePath: ./Dockerfile
    envVars:
      - key: APP_KEY
        generateValue: true
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: APP_URL
        value: https://project-laravel.onrender.com
      - key: LOG_CHANNEL
        value: stderr
      - key: LOG_LEVEL
        value: info
      - key: DB_CONNECTION
        value: pgsql
      # Configurer DB_HOST, DB_PORT, DB_DATABASE, DB_USERNAME, DB_PASSWORD
      # dans le Dashboard Render → Environment (pour base externe)
      - key: CACHE_DRIVER
        value: file
      - key: QUEUE_CONNECTION
        value: database
      - key: SESSION_DRIVER
        value: file
      - key: SESSION_LIFETIME
        value: 120

# Base de données externe (Supabase, ElephantSQL, etc.)
# Configurer les variables DB_* dans le Dashboard Render

# databases:
#   - name: project-laravel-db
#     databaseName: laravel
#     user: laravel
#     plan: free
